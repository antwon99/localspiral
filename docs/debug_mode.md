# Debug Mode

Enable the checkbox on the main interface to print Tyler's perceived grid, the real grid and enemy locations after each `/chat` call. The response includes a `debug` object so the frontend can show this information. Use it to verify map glitches or enemy movement turn by turn.
