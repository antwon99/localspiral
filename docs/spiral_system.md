# Spiral System Overview

The `SpiralSystem` tracks Tyler's mental state during play. Each update applies a
"drift" value that increases `spiral_score` and reduces `sanity`. When the score
reaches 100, Tyler's narration begins to break down.

This simple implementation is a placeholder for future integrations with
embedding drift calculations and hallucination triggers.
